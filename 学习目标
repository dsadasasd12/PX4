根据搜索结果，PX4的姿态控制涉及多个核心源码文件和模块，以下是需要重点学习的文件及其作用分析：

### 一、**多旋翼姿态控制模块**
1. **`mc_att_control_main.cpp`**  
   - **作用**：多旋翼姿态控制的主入口文件，负责订阅传感器数据（如姿态、角速度）、处理控制逻辑（如PID计算）、发布控制指令（如电机输出）。  
   - **关键内容**：  
     - 订阅`control_state`（当前姿态）、`vehicle_attitude_setpoint`（目标姿态）、`sensor_accel`（加速度计数据）等消息。  
     - 实现四元数到旋转矩阵的转换，计算姿态误差，并生成目标角速度。  
   - **源码路径**：`PX4-Autopilot/src/modules/mc_att_control/`。

2. **`AttitudeControl.cpp`**  
   - **作用**：姿态误差计算与四元数处理的核心模块。  
   - **关键内容**：  
     - 通过四元数计算当前姿态与目标姿态的旋转轴和角度误差。  
     - 处理Z轴对齐和偏航角修正，生成外环比例控制的角速度目标值。  
   - **源码路径**：`PX4-Autopilot/src/modules/mc_att_control/AttitudeControl/`。

---

### 二、**固定翼姿态控制模块**
1. **`fw_att_control_main.cpp`**  
   - **作用**：固定翼姿态控制的主文件，实现串级PID控制（外环角度、内环角速度）和舵面混控逻辑。  
   - **关键内容**：  
     - 订阅`vehicle_global_position`（全球位置）、`manual_control_setpoint`（手动控制指令）等消息。  
     - 计算滚转、俯仰和偏航角的PID输出，结合空速缩放系数调整舵面指令。  
   - **源码路径**：`PX4-Autopilot/src/modules/fw_att_control/`。

2. **`ecl_roll_controller.cpp`**  
   - **作用**：固定翼滚转通道的PID控制器实现，属于ECL（Estimation and Control Library）核心库的一部分。  
   - **关键内容**：  
     - 外环角度误差转换为目标角速度，内环角速度误差生成舵面控制量。  
   - **源码路径**：`PX4-Autopilot/src/lib/ecl/attitude_fw/`。

---

### 三、**底层库与工具**
1. **`control_state`和`vehicle_attitude_setpoint`消息定义**  
   - **作用**：姿态控制的关键消息类型，定义在`msg/`目录下的`.msg`文件中。  
   - **关键内容**：  
     - `control_state`包含当前姿态（四元数）、角速度等实时状态。  
     - `vehicle_attitude_setpoint`存储目标姿态和推力指令。  

2. **参数配置文件（`*.params`）**  
   - **作用**：调整PID参数、空速缩放系数等控制逻辑的核心配置。  
   - **关键参数**：  
     - `MC_ROLL_P`、`MC_PITCH_P`（外环比例系数）。  
     - `FW_ACRO_X_MAX`（固定翼最大角速度限制）。  

3. **混控器模块（`mixer`）**  
   - **作用**：将控制指令（如舵面偏转、电机推力）转换为具体的舵机PWM信号。  
   - **源码路径**：`PX4-Autopilot/src/lib/mixer/`。

---

### 四、**仿真与调试工具**
1. **Gazebo模型文件**  
   - **作用**：通过仿真验证姿态控制算法，如四旋翼的动力学模型（`iris.sdf`）。  
   - **关键内容**：  
     - 修改模型参数以匹配实际硬件，测试控制器的鲁棒性。  

2. **日志分析工具（`Flight Review`）**  
   - **作用**：记录飞行数据（如姿态角、角速度、控制输出），用于调参和故障排查。

---

### 五、**扩展学习资料**
- **PX4官方文档**：包含姿态控制流程图、代码结构解析和参数说明。  
- **《零基础入门四旋翼建模与控制（MATLAB仿真）》**：辅助理解控制算法的数学基础。  
- **EKF（扩展卡尔曼滤波）模块**：`AttitudeEstimatorQ`和`ekf2`模块，用于姿态解算。  

---

### 总结
PX4姿态控制的核心文件集中在`mc_att_control`（多旋翼）和`fw_att_control`（固定翼）模块，需结合底层库（如ECL、混控器）和消息订阅机制学习。建议通过仿真（Gazebo）和实际飞行测试验证代码修改效果。
